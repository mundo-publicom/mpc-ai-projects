# ---------------------------------------------------------------------------
# AI — Vercel AI Gateway (routes "provider/model" strings). Optional: with no
# key set, the app returns realistic deterministic mock leads.
# ---------------------------------------------------------------------------
AI_GATEWAY_API_KEY=

# Alternative: a direct Anthropic key also enables the AI path.
# ANTHROPIC_API_KEY=

# ---------------------------------------------------------------------------
# Enrichment providers (optional). Wire real data sources here in production.
# The scaffold uses mock enrichment until these are configured.
# ---------------------------------------------------------------------------
APOLLO_API_KEY=
CLEARBIT_API_KEY=
HUNTER_API_KEY=
PEOPLEDATALABS_API_KEY=
BUILTWITH_API_KEY=

# ---------------------------------------------------------------------------
# CRM export targets (optional).
# ---------------------------------------------------------------------------
HUBSPOT_ACCESS_TOKEN=
SALESFORCE_CLIENT_ID=
SALESFORCE_CLIENT_SECRET=
